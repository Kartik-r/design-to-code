# Generated by design-to-code Component 2. Pattern: microservices


apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-config
data:
  GATEWAY_ENV: "REPLACE_ME"
  
---


apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  labels:
    app: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: REPLACE_WITH_IMAGE_FOR_gateway
          ports:
            - containerPort: 8080
          
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          envFrom:
            - configMapRef:
                name: gateway-config
            

---

apiVersion: v1
kind: Service
metadata:
  name: gateway
spec:
  selector:
    app: gateway
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
---


apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gateway-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 8080
---




apiVersion: v1
kind: Secret
metadata:
  name: service-a-secret
type: Opaque
stringData:
  SERVICE_A_DB_DSN: "REPLACE_ME"
  
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-a
  labels:
    app: service-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-a
  template:
    metadata:
      labels:
        app: service-a
    spec:
      containers:
        - name: service-a
          image: REPLACE_WITH_IMAGE_FOR_service-a
          ports:
            - containerPort: 9001
          
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          envFrom:
            - secretRef:
                name: service-a-secret
            

---

apiVersion: v1
kind: Service
metadata:
  name: service-a
spec:
  selector:
    app: service-a
  ports:
    - port: 9001
      targetPort: 9001
  type: ClusterIP
---





apiVersion: v1
kind: Secret
metadata:
  name: service-b-secret
type: Opaque
stringData:
  SERVICE_B_DB_DSN: "REPLACE_ME"
  
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-b
  labels:
    app: service-b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-b
  template:
    metadata:
      labels:
        app: service-b
    spec:
      containers:
        - name: service-b
          image: REPLACE_WITH_IMAGE_FOR_service-b
          ports:
            - containerPort: 9002
          
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          envFrom:
            - secretRef:
                name: service-b-secret
            

---

apiVersion: v1
kind: Service
metadata:
  name: service-b
spec:
  selector:
    app: service-b
  ports:
    - port: 9002
      targetPort: 9002
  type: ClusterIP
---



