# Generated by design-to-code Component 2. Pattern: event-driven


apiVersion: v1
kind: ConfigMap
metadata:
  name: consumer-config
data:
  BROKER_URL: "REPLACE_ME"
  
---


apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer
  labels:
    app: consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consumer
  template:
    metadata:
      labels:
        app: consumer
    spec:
      containers:
        - name: consumer
          image: REPLACE_WITH_IMAGE_FOR_consumer
          
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          envFrom:
            - configMapRef:
                name: consumer-config
            
      # Depends on external queue topic(s), not managed here (see Terraform):
      #   - orders.created (consume)
      
---




apiVersion: v1
kind: ConfigMap
metadata:
  name: producer-config
data:
  BROKER_URL: "REPLACE_ME"
  
---


apiVersion: apps/v1
kind: Deployment
metadata:
  name: producer
  labels:
    app: producer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: producer
  template:
    metadata:
      labels:
        app: producer
    spec:
      containers:
        - name: producer
          image: REPLACE_WITH_IMAGE_FOR_producer
          ports:
            - containerPort: 8080
          
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          envFrom:
            - configMapRef:
                name: producer-config
            
      # Depends on external queue topic(s), not managed here (see Terraform):
      #   - orders.created (publish)
      
---

apiVersion: v1
kind: Service
metadata:
  name: producer
spec:
  selector:
    app: producer
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
---


apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: producer-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: producer
                port:
                  number: 8080
---


